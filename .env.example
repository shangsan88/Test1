# Server
PORT=3001
JWT_SECRET=change-me-to-something-secret
CLIENT_URL=http://localhost:5173

# Optional SMTP (leave blank to use Ethereal test accounts in dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
